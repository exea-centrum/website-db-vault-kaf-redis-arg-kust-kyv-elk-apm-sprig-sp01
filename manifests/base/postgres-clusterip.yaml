apiVersion: v1
kind: Service
metadata:
  name: postgres-db-normal
  namespace: davtro
  labels:
    app: 
    component: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: 
    component: postgres
